#!/usr/bin/env python

import logging
from s1crets.cli import main
logging.basicConfig(level=logging.INFO, format="%(asctime)s [%(levelname)s] %(name)s/%(threadName)s: %(message)s")

if __name__ == '__main__':
    main()
