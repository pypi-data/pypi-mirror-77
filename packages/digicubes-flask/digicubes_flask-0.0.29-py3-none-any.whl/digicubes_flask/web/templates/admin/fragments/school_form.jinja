{% import "macros/forms.jinja" as forms %}

<form method="POST" action="{{ action }}">
    {{ form.csrf_token }}
    {{ forms.row(form, "name") }}
    {{ forms.row(form, "description") }}
    {{ forms.submit(form) }}
</form>
