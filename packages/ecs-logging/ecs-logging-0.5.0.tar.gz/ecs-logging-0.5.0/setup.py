#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['ecs_logging']

package_data = \
{'': ['*']}

extras_require = \
{":python_version < '3.3'": ['backports.functools-lru-cache'],
 'develop': ['pytest', 'pytest-cov', 'mock', 'structlog', 'elastic-apm']}

setup(name='ecs-logging',
      version='0.5.0',
      description='Logging formatters for ECS (Elastic Common Schema) in Python',
      author='Seth Michael Larson',
      author_email='seth.larson@elastic.co',
      url='https://github.com/elastic/ecs-logging-python',
      packages=packages,
      package_data=package_data,
      extras_require=extras_require,
      python_requires='>=2.7, !=3.0.*, !=3.1.*, !=3.2.*, !=3.3.*, !=3.4.*',
     )
